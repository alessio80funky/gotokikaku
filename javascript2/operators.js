//演算子

/** 演算子のグループ　（ベース）
 
 算術：+ - * / % ** ++ --
 インクリメント・デクリメント：　++ --

 代入：= -= += *= /= %= **=

 比較(関係)：　== !=  === !== < > >= <=
 
 論理：　&& (and) || (or)  ! (not)
 
 */

 // x = a + b

 //xとaとbはオペランドと言います。
 // = と + はオペレーターと言います（演算子）

//その他 typeof

  let str = 10;
  console.log(typeof(str));

  let person = {
    name: "mario",
    age:32,
    job: "engineer"
};

console.log(typeof(person.age))


